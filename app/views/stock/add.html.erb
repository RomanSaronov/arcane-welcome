<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-12">
          <h1>Add Stock</h1>
        </div>
      </div>
      <div class="row bg-light p-3">
        <div class="col-6">
          <!--  Form-->
          <%= form_for @stocks do |f| %>
            <div class="form-group">
              <%= f.label :name %>
              <%= f.text_field :name, class: 'form-group' %>
            </div>
            <div class="form-group">
              <%= f.label :price, 'Unit Price' %>
              <%= f.text_field :price, class: 'form-group' %>
            </div>
            <div class="form-group">
              <%= f.label :interest, 'Unit Price' %>
              <%= f.text_field :interest, class: 'form-group' %>
            </div>
            <div class="form-group">
              <%= f.label :duration, 'Unit Price' %>
              <%= f.text_field :duration, class: 'form-group' %>
            </div>

            <%= f.submit 'Create Account', class: 'btn btn-success %>
          <% end %>
        </div>
        <div class="col-6"></div>
      </div>
    </div>
  </body>
</html>


